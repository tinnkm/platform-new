import { FlexsliderDirective } from './flexslider.directive';

describe('FlexsliderDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexsliderDirective();
    expect(directive).toBeTruthy();
  });
});
